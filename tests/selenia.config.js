module.exports = {
  sources: ['src'], // шляхи до вашого вихідного коду React
  tests: ['tests'], // шляхи до тестів
  platforms: ['web'], // платформа, для якої будуть виконані тести (у цьому випадку - web)
};
